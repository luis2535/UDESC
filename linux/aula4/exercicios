1. gcc -Wall -o prog prog.c 2>&1  | tee gcc.log | grep -c warning

2.  2>&1

3. Pois ele cria primeiro o arquivo que vai ser substituido

4. Ele recriou o arquivo a, para manter o valor precisar usar >>

5. find . -name "*.py" -type f | xargs -I {} sha256sum {} > hashes.txt

6. 

