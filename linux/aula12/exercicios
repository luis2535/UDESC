1) Isso significa que o interpretador /usr/bin/tac será usado para executar o script. No entanto, o comando tac normalmente é usado para inverter a ordem das linhas de um arquivo de texto, exibindo as linhas em ordem reversa.

2) 
