export default class PingTimer {
    private keepalive;
    private timer;
    private checkPing;
    private setTimeout;
    private clearTimeout;
    constructor(keepalive: number, checkPing: () => void);
    private setup;
    clear(): void;
    reschedule(): void;
}
